# 🤖 WebSogood — Discord AI Bot หลายเซิร์ฟเวอร์

บอท Discord ที่แชทด้วย AI ได้ (พร้อมระบบสำรองหลายค่ายอัตโนมัติ) มีระบบดูแลเซิร์ฟเวอร์ครบชุด
และมีฟีเจอร์ที่คิดขึ้นเองไม่เหมือนใคร อย่างระบบ "ความฝันของบอท" และเกมล่าของวิเศษที่ต่อยอดจากมัน

---

## ✨ ฟีเจอร์ทั้งหมด

| หมวด | รายละเอียด |
|---|---|
| 💬 แชท AI | ตอบอัตโนมัติในห้อง/ยศที่กำหนด, จำบทสนทนาต่อเนื่อง, สลับผู้ให้บริการ AI อัตโนมัติเมื่อ Key หมดโควต้า |
| 🎨 สร้างภาพ | `/ai imagine` สร้างภาพจากคำบรรยาย ฟรี ไม่ต้องมี API Key |
| 🏆 ระบบเลเวล | ให้ XP อัตโนมัติ, แจกยศรางวัลตามเลเวล, กระดานผู้นำ |
| 👋 ต้อนรับสมาชิก | ข้อความต้อนรับ/อำลา + Auto-role |
| 🎉 Giveaway | จัดกิจกรรมแจกของรางวัลพร้อมปุ่มกดร่วมกิจกรรม |
| 🏛️ สภา AI | ให้บุคลิก AI 2 ฝ่ายโต้วาทีกันสดๆ พร้อมกรรมการตัดสิน |
| 🔮 คำทำนาย | ผนึกคำทำนายไว้ เปิดเผยเองเมื่อครบเวลา |
| 🛡️ ดูแลเซิร์ฟเวอร์ | kick / ban / timeout / warn / purge ผ่านทั้ง Slash Command และคลิกขวาที่ชื่อสมาชิก (ไม่ต้องพิมพ์คำสั่ง) |
| 🤖 Automod | กรองคำต้องห้าม / ลิงก์ / สแปมเมนชันอัตโนมัติ |
| 🎫 Ticket | ปุ่มเปิดช่องแชทส่วนตัวกับทีมงาน ปิดเองอัตโนมัติ |
| 🚨 Anti-Raid | ตรวจจับคนเข้าร่วมผิดปกติ + แจ้งเตือน/เตะบัญชีใหม่/ยกระดับ Verification ชั่วคราว |
| 🌙 ความฝันของบอท | ทุกวัน AI แต่งเหตุการณ์ในเซิร์ฟเวอร์เป็น "ความฝัน" เชิงสัญลักษณ์ สะสมเป็นตำนานประจำเซิร์ฟเวอร์ (ไม่มีบอทไหนมี) |
| 📜 ธรรมนูญเซิร์ฟเวอร์ | ทุกเหตุการณ์ mod มีโอกาสจุดประกายให้ AI ร่าง "มาตรากฎหมาย" สมมติขำๆ ใหม่ 1 ข้อ |
| 🏺 เกมล่าของวิเศษ | ต่อยอดจากความฝัน — ของวิเศษหลุดจากฝันทุกคืน ให้แย่งคว้า สะสม แลกเปลี่ยน และบูสต์ XP จริง |
| ⚖️ ศาลเซิร์ฟเวอร์ | สมาชิกฟ้องร้องกันขำๆ อ้างอิงธรรมนูญเซิร์ฟเวอร์ได้ AI สวมบทผู้พิพากษาตัดสิน — เกมสมมติเพื่อความบันเทิง |
| 🎛️ แผง Dashboard | ข้อความถาวรในห้องที่กดปุ่มดูข้อมูลได้เอง (เลเวล/ความฝัน/ธรรมนูญ/ของสะสม/สถิติศาล/Ticket) ไม่ต้องพิมพ์คำสั่ง |
| 🧠 ความจำรายบุคคล | AI จดจำนิสัย ความสนใจ และความรู้สึกที่มีต่อผู้ใช้แต่ละคนแยกกัน ใช้เป็นบริบทตอบครั้งถัดไปอย่างเป็นธรรมชาติ |
| 📊 โควต้าโทเคน | นับจำนวนโทเคน AI ที่ใช้จริง (ประมาณการ) และตั้งโควต้าสูงสุดต่อวันต่อเซิร์ฟเวอร์ได้ |

---

## 🔧 การติดตั้ง

```bash
npm install
```

1. คัดลอกไฟล์ `.env` (มีคำอธิบายกำกับทุกบรรทัดอยู่แล้ว) แล้วใส่ค่าจริงของคุณ:

   | ตัวแปร | คำอธิบาย |
   |---|---|
   | `DISCORD_TOKEN` | Token ของบอท จาก [Discord Developer Portal](https://discord.com/developers/applications) → Bot → Reset Token |
   | `OWNER_ID` | Discord User ID ของเจ้าของบอท (เปิด Developer Mode → คลิกขวาโปรไฟล์ตัวเอง → Copy User ID) ใส่ได้หลายคนคั่นด้วย `,` หรือใส่ `ALL` เพื่อให้ทุกคนใช้แผงควบคุมได้ |
   | `PANEL_CMD` | คำสั่งพิมพ์เพื่อเปิดแผงควบคุม (ค่าเริ่มต้น `!panel`) |
   | `PORT` | พอร์ตของ Web Server (บน Render จะกำหนดให้อัตโนมัติ ไม่ต้องแก้) |
   | `MONGODB_URI` | Connection string ของ MongoDB (เช่นจาก [MongoDB Atlas](https://www.mongodb.com/atlas)) |

2. รันบอท:

   ```bash
   npm start          # รันปกติ
   npm run dev        # รันแบบ auto-restart เมื่อแก้โค้ด (node --watch)
   ```

### Deploy บน Render

ใส่ค่าตัวแปรด้านบนใน **Environment** ของ Web Service แทนการสร้างไฟล์ `.env` โดยตรง — โปรเจกต์มี Web Serverในตัว (Express) สำหรับกัน Render พักเครื่อง (sleep) อยู่แล้ว

---

## 📋 คำสั่งทั้งหมด

บอทลงทะเบียนคำสั่งอัตโนมัติทุกเซิร์ฟเวอร์ที่เชิญเข้าไป ไม่ต้องรันสคริปต์ deploy เอง

> ⚠️ **มีการเปลี่ยนแปลงครั้งใหญ่:** คำสั่ง Slash ทั้งหมด (เดิมคือ `/bot` `/ai` `/server` `/community` รวมกว่า 50 subcommand) ถูกยุบเหลือ **แค่ 2 คำสั่ง** เท่านั้น ทุกฟีเจอร์ย้ายไปอยู่หลังปุ่ม/เมนูเลือก/ช่องกรอก (Modal) บนแผงแทนการพิมพ์ subcommand — เซิร์ฟเวอร์ที่เคยใช้คำสั่งเก่า Discord จะลบคำสั่งเก่าออกจากรายการอัตโนมัติหลังบอท re-register (รันบอทเวอร์ชันนี้ครั้งแรกแล้วรอสักครู่)

| คำสั่ง | ใครใช้ได้ | คำอธิบาย |
|---|---|---|
| `/แผงแอดมิน` | ต้องมีสิทธิ์ดูแลเซิร์ฟเวอร์อย่างน้อยหนึ่งอย่าง (Manage Server / Kick / Ban / Moderate Members / Manage Messages) | เปิดแผงศูนย์กลางแอดมิน รวมทุกการตั้งค่า/ดูแลเซิร์ฟเวอร์ไว้ที่เดียว |
| `/เมนู` | ใครก็ใช้ได้ | เปิดแผงส่วนตัว (เห็นเฉพาะคุณ) รวมทุกฟีเจอร์ฝั่งผู้เล่น |

### พิมพ์ด้วย `!` (Prefix Commands — ยังคงใช้ได้เหมือนเดิม)
| คำสั่ง | คำอธิบาย |
|---|---|
| `!panel` (หรือค่าที่ตั้งใน `PANEL_CMD`) | เหมือน `/แผงแอดมิน` — เปิดแผงควบคุมบอทเต็มรูปแบบ |
| `!help` | วิธีใช้งานบอท |
| `!ping` | เช็คว่าบอทออนไลน์อยู่ |
| `!stats` | สถิติการใช้งานแบบเรียลไทม์ |
| `!rank` | เลเวล/XP ของตัวเอง |
| `!leaderboard` | อันดับ XP สูงสุดในเซิร์ฟเวอร์ |
| `!imagine <คำบรรยาย>` | สร้างภาพด้วย AI |
| `!avatar` | สุ่มเปลี่ยนรูปโปรไฟล์บอท (แอดมิน) |
| `!reset` หรือ `!ลืม` | ล้างความจำบทสนทนา + ความทรงจำเกี่ยวกับคุณกับ AI |

### 🧑‍🤝‍🧑 `/เมนู` — ทุกฟีเจอร์ฝั่งผู้เล่น (ใครก็กดได้)

กดปุ่มแล้วบอทจะเด้ง Modal (ช่องกรอก) หรือเมนูเลือกให้เองตามการกระทำ — ปุ่มจะโชว์เฉพาะระบบที่แอดมินเปิดใช้งานอยู่จริงเท่านั้น

| ปุ่มบนแผง | เดิมคือคำสั่ง | คำอธิบาย |
|---|---|---|
| 🧠 ถาม AI | `/ai ask` | ถามคำถามกับ AI โดยตรง (เด้งช่องกรอกคำถาม) |
| 🎨 สร้างภาพ | `/ai imagine` | สร้างภาพด้วย AI (ฟรี ไม่ต้องมี API Key) |
| 🧹 ล้างความจำ AI | `/ai reset` | ล้างความจำบทสนทนา + ความทรงจำเกี่ยวกับคุณที่ AI จำไว้ |
| 🧠 AI จำอะไรเกี่ยวกับฉัน | `/ai memory` | ดูนิสัย/ความสนใจ/ความรู้สึกที่ AI จำเกี่ยวกับคุณ |
| 🏛️ เปิดสภา AI | `/ai council` | สภา AI โต้วาที 2 บุคลิกสดๆ พร้อมกรรมการตัดสิน (เด้งช่องกรอกหัวข้อ) |
| 🔮 ขอคำทำนาย | `/ai prophecy` | คำทำนายผนึกเวลา เปิดเผยเองเมื่อครบเวลา (เด้งช่องกรอกระยะเวลา+หัวข้อ) |
| 🏅 เลเวลของฉัน / 📊 กระดานผู้นำ XP | `/community level rank/leaderboard` | เลเวล/XP และอันดับ |
| 🌙 ความฝันล่าสุด | `/community dream view` | อ่านความฝันล่าสุดที่บอทแต่งขึ้น |
| 📜 ธรรมนูญ / 📖 ดูมาตรา | `/community laws book/article` | เปิดอ่านทั้งเล่ม หรือดูมาตราที่ต้องการ |
| 🏺 คลังของฉัน / 🏆 อันดับนักล่า / 🎁 มอบของวิเศษ | `/community relic inventory/top/gift` | ของวิเศษที่มี, กระดานผู้นำ, มอบให้เพื่อน (เลือกเพื่อนจากเมนู) |
| ⚖️ สถิติศาลของฉัน / 📖 ประวัติคดี / 🧑‍⚖️ ยื่นฟ้องศาล | `/community court record/cases/file` | สถิติ, ประวัติคดี, ยื่นฟ้อง (เลือกจำเลยจากเมนู + กรอกข้อกล่าวหา) |
| 🎫 เปิด Ticket | — | เปิดช่องแชทส่วนตัวกับทีมงาน |
| ❓ วิธีใช้งาน | `/bot info help` | แสดงวิธีใช้งานบอททั้งหมด |

พิมพ์คุยกับบอทตรงๆ ในห้องที่กำหนดก็ยังได้เหมือนเดิม (บอทจำบทสนทนาต่อเนื่อง + จดจำนิสัย/ความรู้สึกที่มีต่อคุณ)

### 🎛️ `/แผงแอดมิน` — ทุกฟีเจอร์ฝั่งแอดมิน/ดูแลเซิร์ฟเวอร์

หน้าแรกคือ "ศูนย์กลาง" ที่มีปุ่มพาไปแต่ละหมวด — แต่ละหมวดเช็คสิทธิ์ Discord ของตัวเองตอนกด (ไม่ใช่แค่ตอนเปิดคำสั่ง) เพื่อให้มอดที่มีสิทธิ์เฉพาะทาง (เช่น Kick Members อย่างเดียว) ยังใช้หน้าที่เกี่ยวข้องได้:

| หมวดบนแผง | สิทธิ์ที่ต้องมี | เดิมคือคำสั่ง | ครอบคลุม |
|---|---|---|---|
| 🧠 ตั้งค่า AI | เจ้าของบอท/แอดมิน (ตั้งค่าใน "สิทธิ์การใช้") | `/bot info panel` | prompt, API key, cooldown, รูปแบบคำตอบ, สี Embed, ระบบเลเวล, ต้อนรับ/อำลา, สิทธิ์การใช้, ขั้นสูง, สถิติ |
| 🛡️ ดูแลเซิร์ฟเวอร์ | Kick/Ban/Moderate Members/Manage Messages (ตามปุ่มที่กด) | `/server mod` | เตะ, แบน, ปลดแบน, timeout, ยกเลิก timeout, เตือน, ดูประวัติเตือน, ล้างประวัติเตือน, ลบข้อความจำนวนมาก |
| 🧹 Automod | Manage Server | `/server automod` | เปิด/ปิด, บล็อกลิงก์/เชิญ, จำกัดเมนชัน, แก้ไขคำต้องห้ามทั้งชุด |
| 🎫 Ticket | Manage Server | `/server ticket setup` | เลือกหมวดหมู่+ยศทีมงาน แล้วโพสต์ปุ่มเปิด Ticket |
| 🚨 Anti-Raid | Manage Server | `/server antiraid` | เปิด/ปิด, ตั้งเกณฑ์, เลือกการดำเนินการ, ปลดล็อก Verification |
| 🌙 ระบบชุมชน | Manage Server (giveaway ต้องเป็นเจ้าของบอท/แอดมิน) | `/community dream\|laws\|relic\|court\|giveaway` | เปิด/ปิดความฝัน/ธรรมนูญ/ของวิเศษ/ศาล, เลือกห้อง, ให้บอทฝันตอนนี้, เริ่ม/จบ/สุ่มผู้ชนะกิจกรรม |
| 🏆 เลเวล / 👋 ต้อนรับ-อำลา / 🚫 สิทธิ์การใช้ / ⚙️ ขั้นสูง / 📊 สถิติ | เจ้าของบอท/แอดมิน | `/bot info panel` (หน้าย่อยเดิม) | เหมือนเดิมทุกอย่าง ไม่มีการเปลี่ยนแปลง |
| 📌 ปักหมุดแผงสมาชิก / 🔄 อัปเดตแผงที่ปักไว้ | Manage Server | `/bot dashboard setup/refresh` | เผยแพร่ปุ่มแบบ `/เมนู` ถาวรลงห้องที่เลือก ให้สมาชิกกดได้โดยไม่ต้องพิมพ์คำสั่ง |

> 💡 ล่าของวิเศษ (🏺) ต้องเลือกห้องความฝันก่อน เพราะของวิเศษหลุดออกมาในห้องเดียวกับความฝัน

**คลิกขวาที่ชื่อสมาชิก → Apps** ก็ใช้ เตะ/แบน/Timeout/เตือน/ดูประวัติเตือน ได้ทันทีโดยไม่ต้องเปิดแผงเลย (คนละ UI surface กับ Slash Command เลยไม่ถูกรวมเข้า `/แผงแอดมิน`)

---

## 🛠️ หมายเหตุ: แก้ปัญหาคำสั่งซ้ำ/คำสั่งผีค้างเมนู

เดิมโปรเจกต์นี้ลงทะเบียนคำสั่งทั้งแบบ **Global** (`client.application.commands.set(...)`) และแบบ **เฉพาะเซิร์ฟเวอร์** (`guild.commands.set(...)`) พร้อมกัน
ถ้าการอัปเดต Global ล้มเหลว (เช่นตอนคำสั่งมีจำนวนมากขึ้นเรื่อยๆ) รายการ Global เก่าจะค้างอยู่และไปซ้อนกับคำสั่งเฉพาะเซิร์ฟเวอร์
ทำให้เห็นคำสั่งซ้ำหรือคำสั่งเก่าที่ไม่มีในโค้ดแล้ว (ผี) โผล่มาในเมนูพิมพ์คำสั่ง

ตอนนี้แก้แล้วโดย **ลบการลงทะเบียนแบบ Global ออกทั้งหมด** (ไม่จำเป็นอยู่แล้ว เพราะคำสั่งเฉพาะเซิร์ฟเวอร์ครอบคลุมทั้งเซิร์ฟเวอร์เดิมและเซิร์ฟเวอร์ใหม่อยู่แล้ว)
และเพิ่มการล้าง Global Commands เก่าทิ้งครั้งเดียวตอนบอทสตาร์ท (`client.application.commands.set([])`) — รันบอทเวอร์ชันนี้ครั้งแรกแล้วรอสักครู่ คำสั่งซ้ำ/คำสั่งผีจะหายไปเอง (รวมถึงคำสั่งเก่า `/bot` `/ai` `/server` `/community` จากเวอร์ชันก่อนหน้าด้วย)

## 🐛 หมายเหตุ: แก้บั๊ก Giveaway/Prophecy ไม่หมดเวลาอัตโนมัติ

ระหว่างตรวจสอบโค้ดทั้งหมดพบว่า `giveawayCache` และ `propheciesCache` (ตัวแปรภายในของ `lib/storage.js` ที่ใช้เช็คว่ากิจกรรม/คำทำนายไหนครบเวลาแล้วบ้าง) **ไม่เคยถูก export ออกมาให้ `index.js` ใช้งานเลย** ตั้งแต่ต้น
ทำให้ตัวจับเวลาที่เช็คทุก 20-30 วินาที error เงียบๆ ทุกรอบ (ถูกดักไว้โดย `process.on('uncaughtException', ...)` เลยไม่ทำให้บอทล่ม แต่ก็แปลว่า **Giveaway ไม่เคยจบอัตโนมัติ และคำทำนายไม่เคยเปิดผนึกเองเลย** ต้องรอให้แอดมินกดปุ่ม "🏁 จบกิจกรรม" บนแผงเอาเองตลอดมา)

แก้แล้วโดยเพิ่ม `giveawayCache` และ `propheciesCache` เข้าไปใน `module.exports` ของ `lib/storage.js` — ตอนนี้ทั้งสองระบบจะจบ/เปิดผนึกอัตโนมัติตามเวลาที่ตั้งไว้แล้ว

---

## 🧠 ความจำรายบุคคล (Per-user Relationship Memory)

นอกจากความจำบทสนทนาแบบชั่วคราว (จำแค่ระหว่างคุยต่อเนื่อง แล้วลืมเมื่อหยุดคุยไป 30 นาที) บอทยังมีความจำ **ถาวรแยกรายบุคคล** เก็บไว้ใน MongoDB:

- ทุกๆ 4 รอบสนทนา บอทจะให้ AI สรุป "นิสัย ความสนใจ และแนวโน้มน้ำเสียง" ของผู้ใช้คนนั้นเป็นบันทึกสั้นๆ พร้อมประเมินความรู้สึกที่มีต่อบอท (พอใจ/เฉยๆ/ไม่พอใจ/โกรธ)
- บันทึกนี้จะถูกดึงกลับมาใส่ในบริบทของบอทเวลาคุยกับคนคนนั้นครั้งต่อไป ทำให้บอท "จำได้" อย่างเป็นธรรมชาติ โดยไม่ต้องพูดตรงๆ ว่า "ระบบจำไว้ว่า..."
- ผู้ใช้ดูของตัวเองได้ด้วย `/ai memory` และล้างทิ้งได้ตลอดเวลาด้วย `/ai reset` (ล้างทั้งความจำบทสนทนาและบันทึกความสัมพันธ์)
- ปิดได้ทั้งระบบผ่านสวิตช์ `memoryEnabled` เดิม (ถ้าปิดความจำบทสนทนา ก็จะไม่เก็บความสัมพันธ์ด้วยเช่นกัน)

## 📊 การนับ + จำกัดโควต้าโทเคน AI

เนื่องจากไม่มี tokenizer จริงติดตั้งอยู่ในระบบ (ไม่ได้ต่ออินเทอร์เน็ตตอนพัฒนา) จึงใช้สูตร **ประมาณการแบบ conservative**: ภาษาไทย ~1 โทเคน/2.5 ตัวอักษร, ภาษาอังกฤษ/ตัวเลข ~1 โทเคน/4 ตัวอักษร ตัวเลขนี้ใช้เพื่อ 2 อย่าง:

1. **ตัดความจำบทสนทนาตามโทเคนจริง** (ไม่ใช่แค่จำนวนข้อความ) กันส่ง context ยาวเกินจนผู้ให้บริการ AI ปฏิเสธ — ปรับได้ผ่าน `memoryMaxTokens` (ค่าเริ่มต้น 3000)
2. **จำกัดโควต้ารายวันต่อเซิร์ฟเวอร์** — แอดมินตั้งได้ด้วย `/ai limit` (0 = ไม่จำกัด) ดูการใช้งานปัจจุบันได้ด้วย `/ai usage` เมื่อใช้ครบโควต้า บอทจะหยุดตอบชั่วคราวจนกว่าจะรีเซ็ตในวันถัดไป

---

## 🗄️ โครงสร้างโปรเจกต์

```
index.js          # เชื่อมต่อ Discord จริง (Client, Event handlers, Slash/Context Menu Commands)
lib/
  storage.js       # โมเดลข้อมูล MongoDB ทั้งหมด + ฟังก์ชันอ่าน-เขียน
  ai.js            # เรียก AI หลายค่ายพร้อมระบบสำรองอัตโนมัติ
  panels.js        # UI แผงควบคุม (Embed/Button/Select Menu)
test/run-all.js   # เทสพื้นฐาน
```

ข้อมูลทั้งหมดเก็บใน MongoDB (Mongoose) — ตั้งค่าเซิร์ฟเวอร์แยกกันเป็นรายกิลด์ ปรับผ่านแผงควบคุมหรือ Slash Command ได้ทั้งหมด ไม่ต้องแก้โค้ด
